[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# If deploy artifacts grow too large/time out, consider:
# command = "npm run build && rm -rf .next/cache"
